version: '3.8'

services:
  # Config initialization containers
  prometheus-config-init:
    image: alpine:latest
    container_name: prometheus-config-init
    volumes:
      - prometheus_config:/config
      - ./prometheus:/src:ro
    command: sh -c "cp -r /src/* /config/ && chmod -R 644 /config/*"
    restart: "no"

  alertmanager-config-init:
    image: alpine:latest
    container_name: alertmanager-config-init
    volumes:
      - alertmanager_config:/config
      - ./alertmanager:/src:ro
    command: sh -c "cp -r /src/* /config/ && chmod -R 644 /config/*"
    restart: "no"

  grafana-config-init:
    image: alpine:latest
    container_name: grafana-config-init
    volumes:
      - grafana_config:/config
      - ./grafana/provisioning:/src:ro
    command: sh -c "cp -r /src/* /config/ && chmod -R 644 /config/*"
    restart: "no"

  rabbitmq:
    image: rabbitmq:3-management
    container_name: notrus-rabbitmq
    hostname: rabbitmq
    environment:
      RABBITMQ_DEFAULT_USER: notrus
      RABBITMQ_DEFAULT_PASS: notrus123
      RABBITMQ_DEFAULT_VHOST: /
    ports:
      - "5672:5672"
      - "15672:15672"
    volumes:
      - rabbitmq_data:/var/lib/rabbitmq
    networks:
      - notrus-network
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "rabbitmq-diagnostics", "check_port_connectivity"]
      interval: 30s
      timeout: 10s
      retries: 5

  postgres:
    image: postgres:15
    container_name: notrus-postgres
    environment:
      POSTGRES_DB: notrus
      POSTGRES_USER: notrus
      POSTGRES_PASSWORD: notrus123
      PGDATA: /var/lib/postgresql/data/pgdata
    ports:
      - "5433:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    networks:
      - notrus-network
    restart: unless-stopped
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U notrus"]
      interval: 30s
      timeout: 10s
      retries: 5

  prometheus:
    image: prom/prometheus:latest
    container_name: notrus-prometheus
    depends_on:
      - prometheus-config-init
    ports:
      - "9090:9090"
    volumes:
      - prometheus_config:/etc/prometheus:ro
      - prometheus_data:/prometheus
    command:
      - '--config.file=/etc/prometheus/prometheus.yml'
      - '--storage.tsdb.path=/prometheus'
      - '--web.console.libraries=/etc/prometheus/console_libraries'
      - '--web.console.templates=/etc/prometheus/consoles'
      - '--storage.tsdb.retention.time=200h'
      - '--web.enable-lifecycle'
    networks:
      - notrus-network
    restart: unless-stopped

  grafana:
    image: grafana/grafana:latest
    container_name: notrus-grafana
    depends_on:
      - grafana-config-init
    ports:
      - "3001:3000"
    environment:
      - GF_SECURITY_ADMIN_USER=admin
      - GF_SECURITY_ADMIN_PASSWORD=notrus123
      - GF_USERS_ALLOW_SIGN_UP=false
    volumes:
      - grafana_data:/var/lib/grafana
      - grafana_config:/etc/grafana/provisioning:ro
    networks:
      - notrus-network
    restart: unless-stopped

  alertmanager:
    image: prom/alertmanager:latest
    container_name: notrus-alertmanager
    depends_on:
      - alertmanager-config-init
    ports:
      - "9093:9093"
    volumes:
      - alertmanager_config:/etc/alertmanager:ro
      - alertmanager_data:/alertmanager
    command:
      - '--config.file=/etc/alertmanager/alertmanager.yml'
      - '--storage.path=/alertmanager'
      - '--web.external-url=http://localhost:9093'
    networks:
      - notrus-network
    restart: unless-stopped

volumes:
  rabbitmq_data:
  postgres_data:
  prometheus_data:
  prometheus_config:
  grafana_data:
  grafana_config:
  alertmanager_data:
  alertmanager_config:

networks:
  notrus-network:
    driver: bridge